name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl linker
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build server binary
        run: cargo build --release --target x86_64-unknown-linux-musl -p feditexter-server

      - name: Strip binary
        run: strip target/x86_64-unknown-linux-musl/release/feditexter-server

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: target/x86_64-unknown-linux-musl/release/feditexter-server
          generate_release_notes: true